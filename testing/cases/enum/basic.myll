module basic;

enum Test {
	A,
	B,
	C
}

enum TestComma {
	A,
	B,
	C,
}

[inline]
func basic_test() -> int
{
	var Test basic = Test::A;
	var TestComma basicComma = TestComma::B;

	if( (int)(basic) != 0 || (int)(basicComma) != 1 )
		return 1;

	return 0;
}
