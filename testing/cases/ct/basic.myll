module basic;

func basic_test() -> int
{
	[ct] const int answer = 42;
	[ct] var int doubled = answer * 2;

	static_assert(answer == 42, "answer must be 42");
	static_assert(doubled == 84, "doubled must be 84");

	if( answer != 42 )
		return 1;

	if( doubled != 84 )
		return 2;

	return 0;
}
